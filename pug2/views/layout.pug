doctype html
html(lang='en')
  head
    title= title
    style(type="text/css").
      body { 
        color: green;
        font-family: Helvetica, Arial;
        font-weight: bold;
        font-size: 3;
      }
    block scripts
  body
    block content
    block foot